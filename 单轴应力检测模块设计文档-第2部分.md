# 单轴应力检测模块设计文档 v1.0 - 第2部分

> **包含章节**：第4章 形状定义功能、第5章 布点策略、第6章 采集流程  
> **上一部分**：[第1.5-B部分](./单轴应力检测模块设计文档-第1.5-B部分.md)  
> **下一部分**：[第3部分](./单轴应力检测模块设计文档-第3部分.md)

---

## 4. 形状定义功能

### 4.1 矩形模式

#### 参数定义
```python
shape_config = {
    "type": "rectangle",
    "width": 100,      # mm
    "height": 150,     # mm
    "modifiers": []    # 布尔运算列表
}
```

#### 界面操作
```
试件形状: [●矩形] [○圆形] [○自定义]

基础参数:
  宽度 (mm): [100]
  高度 (mm): [150]

形状编辑 (可选):
  ☑ 启用布尔运算
  
  [+ 添加孔洞/缺口]  [+ 添加凸起]
  
  已添加:
  ● 圆形孔 - 中心(50,75) 半径20mm [编辑] [删除]
  ● 矩形缺口 - 左上(0,60) 宽20 高30 [编辑] [删除]
```

#### 布尔运算类型
- **减去（孔洞/缺口）**：
  - 圆形孔：中心坐标 + 半径
  - 矩形槽：左上角坐标 + 宽高
  - U型缺口：位置 + 尺寸
  
- **添加（凸起）**：
  - 矩形凸台：位置 + 尺寸
  - 圆形凸起：中心 + 半径

### 4.2 圆形模式

#### 参数定义
```python
shape_config = {
    "type": "circle",
    "center_x": 50,
    "center_y": 50,
    "outer_radius": 40,
    "inner_radius": 0,      # 0表示实心圆
    "start_angle": 0,       # 0表示完整圆
    "end_angle": 360,
    "modifiers": []
}
```

#### 界面操作
```
试件形状: [○矩形] [●圆形] [○自定义]

基础参数:
  圆心 X (mm): [50]
  圆心 Y (mm): [50]
  外半径 (mm): [40]

检测区域限制:
  ☑ 启用扇形区域
    起始角度 (°): [0]
    结束角度 (°): [90]
  
  ☑ 启用圆环区域
    内半径 (mm): [10]
    (不检测圆心区域)

形状编辑:
  [+ 添加孔洞]  [+ 添加缺口]
```

#### 典型应用场景
- **圆盘**：outer_radius=50, inner_radius=0
- **圆环**：outer_radius=50, inner_radius=20
- **扇形**：outer_radius=50, start_angle=0, end_angle=90
- **圆环扇形**：组合上述参数

### 4.3 自定义多边形模式

#### 参数定义
```python
shape_config = {
    "type": "polygon",
    "vertices": [
        [0, 0],
        [100, 0],
        [100, 50],
        [50, 50],
        [50, 100],
        [0, 100]
    ],
    "modifiers": []
}
```

#### 界面操作
```
试件形状: [○矩形] [○圆形] [●自定义]

绘制方式:
  ○ 点击画布添加顶点
  ○ 输入坐标

顶点列表:
  #1: (0, 0)      [删除] [↑] [↓]
  #2: (100, 0)    [删除] [↑] [↓]
  #3: (100, 50)   [删除] [↑] [↓]
  #4: (50, 50)    [删除] [↑] [↓]
  #5: (50, 100)   [删除] [↑] [↓]
  #6: (0, 100)    [删除] [↑] [↓]
  
  [+ 添加顶点] [闭合轮廓] [清空]
  
提示: 在右侧画布上点击添加顶点
```

#### 交互方式
1. **点击添加**：在预览画布上点击，自动记录坐标
2. **手动输入**：直接输入坐标值
3. **拖拽调整**：拖动顶点修改位置
4. **顺序调整**：使用 ↑↓ 按钮调整顶点顺序

### 4.4 形状验证与预览

#### 实时预览
- 在右侧画布实时显示形状轮廓
- 孔洞/缺口用虚线或不同颜色标注
- 显示关键尺寸标注

#### 有效性检查
```python
def validate_shape(shape_config):
    """验证形状配置的有效性"""
    checks = {
        "面积检查": area > 0,
        "边界检查": all_points_within_bounds,
        "孔洞检查": holes_within_shape,
        "自相交检查": no_self_intersection
    }
    return checks
```

#### 错误提示
- ❌ 形状面积为零或负值
- ❌ 孔洞超出试件边界
- ❌ 多边形自相交
- ⚠️ 有效测量区域过小（< 10% 总面积）

---

## 5. 布点策略

### 5.1 网格布点（最常用）

#### 参数设置
```
布点方式: [网格布点 ▼]

网格参数:
  行数: [5]
  列数: [5]
  行间距 (mm): [10] [变距]
  列间距 (mm): [10] [变距]
  边距 (mm): [10]
  
[预览布点] [生成测点]
```

#### 变距输入弹窗
点击"变距"按钮后弹出输入框:

**行间距变距设置**
```
行间距数组 (共4个间距，5行需要4个间距):
  第1-2行间距 (mm): [10]
  第2-3行间距 (mm): [15]
  第3-4行间距 (mm): [15]
  第4-5行间距 (mm): [10]

[确定] [取消] [恢复等距]
```

**列间距变距设置**
```
列间距数组 (共4个间距，5列需要4个间距):
  第1-2列间距 (mm): [10]
  第2-3列间距 (mm): [12]
  第3-4列间距 (mm): [12]
  第4-5列间距 (mm): [10]

[确定] [取消] [恢复等距]
```

#### 生成算法
```python
def generate_grid_points(shape_config, rows, cols, row_spacings, col_spacings, margin):
    """生成网格测点（支持变间距）
    
    Args:
        shape_config: 形状配置
        rows: 行数
        cols: 列数
        row_spacings: 行间距数组 [s1, s2, ..., s(n-1)] 或单个值（等距）
        col_spacings: 列间距数组 [s1, s2, ..., s(m-1)] 或单个值（等距）
        margin: 边距
    """
    # 处理等距情况
    if isinstance(row_spacings, (int, float)):
        row_spacings = [row_spacings] * (rows - 1)
    if isinstance(col_spacings, (int, float)):
        col_spacings = [col_spacings] * (cols - 1)
    
    # 计算起始位置
    x_start = margin
    y_start = margin
    
    # 生成Y坐标（行）
    y_coords = [y_start]
    for spacing in row_spacings:
        y_coords.append(y_coords[-1] + spacing)
    
    # 生成X坐标（列）
    x_coords = [x_start]
    for spacing in col_spacings:
        x_coords.append(x_coords[-1] + spacing)
    
    # 生成测点
    points = []
    index = 1
    for y in y_coords:
        for x in x_coords:
            # 检查点是否在有效区域内（排除孔洞）
            if is_point_valid(x, y, shape_config):
                points.append({
                    'id': index,
                    'x': round(x, 2),
                    'y': round(y, 2),
                    'status': 'pending'
                })
                index += 1
    
    return points
```

#### 示例效果

**等距网格**
```
试件 100×150 mm，5×5 网格，等距 10mm，边距 10mm

● ● ● ● ●
● ● ○ ● ●  ← ○ 表示孔洞位置，自动跳过
● ● ● ● ●
● ● ● ● ●
● ● ● ● ●

生成 24 个有效测点（1个被孔洞排除）
```

**变距网格（应力集中区域加密）**
```
试件 100×150 mm，5×5 网格，变距设置
行间距: [10, 5, 5, 10] mm
列间距: [10, 5, 5, 10] mm

●   ●   ●   ●   ●
  
●   ● ● ● ●   ●
    ↑ 中间区域加密
●   ● ● ● ●   ●

●   ●   ●   ●   ●

适用于中心区域应力集中的场景
```

### 5.2 极坐标布点

#### 参数设置
```
布点方式: [极坐标布点 ▼]

圆心位置:
  X 坐标 (mm): [50]  💡点击画布设置
  Y 坐标 (mm): [50]

径向参数:
  径向层数: [3]
  起始半径 (mm): [0]
  半径步长 (mm): [10]
  
角度范围:
  起始角度 (°): [0]
  结束角度 (°): [360]
  
角度分布:
  ○ 固定角度步长: [45] °
  ● 固定每层点数: [8] [每层不同]
  
[预览布点] [生成测点]
```

**说明**：
- **圆心坐标**：可手动输入或点击预览画布设置
- **半径步长**：相邻两层圆环之间的径向距离
- **角度范围**：决定是完整圆(360°)、半圆(180°)还是扇形
- **角度分布**：两种模式二选一
  - 固定角度步长：每层点数自动计算 = (结束角度 - 起始角度) / 角度步长
  - 固定每层点数：角度步长自动计算 = (结束角度 - 起始角度) / 点数

#### 每层不同点数弹窗
点击"每层不同"按钮后弹出输入框:

**每层点数设置**
```
每层点数配置 (共3层):
  第1层点数: [6]
  第2层点数: [8]
  第3层点数: [12]

提示: 通常外层点数 ≥ 内层点数

[确定] [取消] [恢复统一]
```

#### 生成算法
```python
def generate_polar_points(center_x, center_y, r_start, r_step, n_layers, 
                         start_angle, end_angle, 
                         angle_mode='fixed_points', angle_param=8):
    """生成极坐标测点（支持角度范围和两种角度分布模式）
    
    Args:
        center_x, center_y: 圆心坐标
        r_start: 起始半径 (mm)
        r_step: 半径步长 (mm)
        n_layers: 径向层数
        start_angle: 起始角度 (度)
        end_angle: 结束角度 (度)
        angle_mode: 角度分布模式
            - 'fixed_step': 固定角度步长
            - 'fixed_points': 固定每层点数
        angle_param: 角度参数
            - 当 angle_mode='fixed_step' 时，为角度步长 (度)
            - 当 angle_mode='fixed_points' 时，为每层点数或点数数组
    
    Returns:
        list: 测点列表 [{"id": int, "x": float, "y": float, "r": float, "theta": float}, ...]
    """
    points = []
    index = 1
    
    # 计算角度范围
    angle_range = end_angle - start_angle
    
    # 处理每层点数
    if angle_mode == 'fixed_step':
        # 固定角度步长模式：每层点数自动计算
        angle_step = angle_param
        n_points_per_layer = int(angle_range / angle_step)
        points_per_layer = [n_points_per_layer] * n_layers
    else:
        # 固定每层点数模式：角度步长自动计算
        if isinstance(angle_param, int):
            # 统一点数
            points_per_layer = [angle_param] * n_layers
        else:
            # 每层不同点数
            points_per_layer = angle_param
    
    # 生成各层测点
    for layer_idx in range(n_layers):
        # 计算当前层半径
        radius = r_start + layer_idx * r_step
        
        # 当前层点数
        n_points = points_per_layer[layer_idx]
        
        # 计算当前层角度步长
        if n_points > 0:
            actual_angle_step = angle_range / n_points
        else:
            continue
        
        # 生成当前层的点
        for point_idx in range(n_points):
            angle_deg = start_angle + point_idx * actual_angle_step
            angle_rad = np.radians(angle_deg)
            
            x = center_x + radius * np.cos(angle_rad)
            y = center_y + radius * np.sin(angle_rad)
            
            if is_point_valid(x, y, shape_config):
                points.append({
                    'id': index,
                    'x': round(x, 2),
                    'y': round(y, 2),
                    'r': round(radius, 2),
                    'theta': round(angle_deg % 360, 2)  # 归一化到 0-360
                })
                index += 1
    
    return points
```

#### 示例效果

**统一点数**
```
圆盘试件，3层，每层8点，起始半径0mm，步长10mm

      ●
   ●  ●  ●
 ●    ●    ●
●  ●  ●  ●  ●
 ●    ●    ●
   ●  ●  ●
      ●

生成 24 个测点（3层 × 8点）
```

**每层不同点数（外层加密）**
```
圆盘试件，3层，点数 [6, 8, 12]
起始半径0mm，步长10mm

        ●
    ●   ●   ●
  ●   ●   ●   ●
 ●  ●  ●  ●  ●  ●
  ●   ●   ●   ●
    ●   ●   ●
        ●

生成 26 个测点（6 + 8 + 12）
外层点数更多，角度分辨率更高
```

### 5.3 变间距布点

#### 参数设置
```
布点方式: [变间距布点 ▼]

试件尺寸:
  宽度 (mm): [100]
  高度 (mm): [150]

密集区域 (应力集中):
  中心 X (mm): [50]
  中心 Y (mm): [30]
  密集半径 (mm): [20]
  密集区测点数: [16]

稀疏区域:
  外围测点数: [12]
  
[预览布点] [生成测点]
```

#### 生成算法
```python
def generate_adaptive_points(shape_config, dense_center, dense_radius, 
                            n_dense, n_sparse):
    """生成变间距测点"""
    points = []
    
    # 密集区域：小网格
    dense_points = generate_grid_in_circle(
        dense_center, dense_radius, n_dense
    )
    
    # 稀疏区域：大网格（排除密集区）
    sparse_points = generate_sparse_grid(
        shape_config, exclude_circle=(dense_center, dense_radius), 
        n_points=n_sparse
    )
    
    return dense_points + sparse_points
```

#### 应用场景
- 孔洞周围应力集中
- 裂纹尖端区域
- 载荷作用点附近
- 边界效应区域

### 5.4 自定义布点

#### 操作方式
```
布点方式: [自定义布点 ▼]

操作方式:
  ● 点击画布添加
  ○ 导入坐标文件

测点列表:
  #1: (10.0, 10.0)  [删除] [编辑]
  #2: (50.0, 50.0)  [删除] [编辑]
  #3: (80.0, 30.0)  [删除] [编辑]
  
[在画布上点击添加] [导入 CSV] [清空]
```

#### CSV 导入格式
```csv
x,y
10.0,10.0
50.0,50.0
80.0,30.0
```

#### 交互添加
- 在预览画布上点击 → 自动添加测点
- 拖拽测点 → 调整位置
- 右键点击 → 删除测点

### 5.5 测点排序优化

#### 排序策略
```python
def optimize_point_order(points, strategy='zigzag'):
    """优化测点顺序，减少移动距离"""
    if strategy == 'zigzag':
        # 之字形扫描
        return zigzag_sort(points)
    elif strategy == 'nearest':
        # 最近邻算法（类似TSP）
        return nearest_neighbor_sort(points)
    elif strategy == 'spiral':
        # 螺旋扫描
        return spiral_sort(points)
```

#### 示例：之字形扫描
```
1→2→3→4→5
          ↓
10←9←8←7←6
↓
11→12→13→14→15

减少探头移动距离，提高效率
```

---

## 6. 采集流程

### 6.1 工作流程图

```
[开始] 
  ↓
[加载标定数据] → 选择标定实验和方向
  ↓
[定义试件形状] → 矩形/圆形/自定义
  ↓
[生成测点布局] → 网格/极坐标/变间距
  ↓
[设置基准测点] → 默认 #1，可修改
  ↓
[开始采集] ────┐
  ↓            │
[移动探头到测点 #N] ← 用户手动操作
  ↓            │
[采集波形]     │
  ↓            │
[计算时间差]   │
  ↓            │
[计算应力值]   │
  ↓            │
[保存数据]     │
  ↓            │
[更新云图]     │
  ↓            │
[N < 总数?] ───┘ 是 → 继续下一测点
  ↓ 否
[生成最终云图]
  ↓
[导出报告]
  ↓
[结束]
```

### 6.2 状态管理

#### 测点状态
```python
class PointStatus:
    PENDING = 'pending'      # 待测
    MEASURING = 'measuring'  # 测量中
    MEASURED = 'measured'    # 已测
    SKIPPED = 'skipped'      # 已跳过
    ERROR = 'error'          # 测量失败
```

#### 实验状态
```python
class ExperimentStatus:
    PLANNING = 'planning'    # 规划阶段（配置形状、布点）
    READY = 'ready'          # 准备就绪（配置完成，未开始采集）
    RUNNING = 'running'      # 采集中（正在采集）
    PAUSED = 'paused'        # 已暂停（可继续）
    COMPLETED = 'completed'  # 已完成（用户点击"完成实验"）
    ABORTED = 'aborted'      # 已中止（提前终止）
```

#### 数据保存时机

**自动保存机制**：
```python
# 1. 创建实验时
create_experiment() → 立即在 SQLite 创建记录

# 2. 配置修改时
update_shape_config() → 自动保存到 SQLite
update_point_layout() → 自动保存到 SQLite

# 3. 每采集一个测点
capture_single_point() → 立即保存到 HDF5 + SQLite
    - 波形数据 → HDF5 文件
    - 应力值、时间差 → SQLite 表
    - 更新实验进度 → SQLite

# 4. 点击"完成实验"
complete_experiment() → 标记状态为 'completed'
    - 生成最终云图
    - 锁定实验数据（不可再修改）
    - 可选：生成实验报告
```

**"完成实验"按钮功能**：
- 标记实验为"已完成"状态
- 生成最终高质量云图（使用所有测点数据）
- 锁定实验数据，防止误修改
- 可选：自动生成实验报告PDF
- 实验完成后仍可查看、导出，但不可修改

**数据恢复能力**：
- 意外关闭程序：重新打开后可加载实验，继续采集
- 部分采集完成：已采集的测点数据已保存，可继续剩余测点
- 实验中断：可随时暂停，稍后继续

### 6.3 采集控制逻辑

#### 单点采集流程
```python
async def capture_single_point(point_id):
    """采集单个测点"""
    try:
        # 1. 更新状态
        update_point_status(point_id, 'measuring')
        
        # 2. 提示用户移动探头
        show_message(f"请将探头移动到测点 #{point_id}")
        
        # 3. 采集波形
        waveform = await capture_waveform_from_scope()
        
        # 4. 与基准波形互相关
        baseline = load_baseline_waveform()
        time_diff = calculate_cross_correlation(baseline, waveform)
        
        # 5. 计算应力值
        calibration = load_calibration_data()
        stress = calculate_stress(time_diff, calibration)
        
        # 6. 保存数据
        save_point_data(point_id, waveform, time_diff, stress)
        
        # 7. 更新状态
        update_point_status(point_id, 'measured')
        
        # 8. 刷新云图
        update_contour_map()
        
        return {'success': True, 'stress': stress}
        
    except Exception as e:
        update_point_status(point_id, 'error')
        return {'success': False, 'error': str(e)}
```

#### 自动模式
```python
async def auto_capture_mode():
    """自动采集模式"""
    for point in pending_points:
        # 采集当前点
        result = await capture_single_point(point.id)
        
        if result['success']:
            # 自动进入下一测点
            if auto_advance_enabled:
                await asyncio.sleep(1)  # 短暂延迟
                continue
            else:
                # 等待用户确认
                await wait_for_user_confirmation()
        else:
            # 异常处理
            if auto_retry_enabled:
                result = await capture_single_point(point.id)
            else:
                # 询问用户操作
                action = await ask_user_action()  # 重试/跳过/中止
                if action == 'abort':
                    break
```

### 6.4 基准波形管理

#### 设计原则

**基准波形的重要性**：
- 所有应力计算都是相对于基准波形的时间差
- 基准波形质量直接影响整个实验的准确性
- 基准波形应在无应力或已知应力状态下采集

**基准波形存储位置**：
- HDF5 文件中的 `baseline` 组
- 包含完整波形数据和元数据

#### 基准波形设置

**默认行为**：
- 实验开始时，第一个测点（通常是 #1）自动设为基准点
- 采集第一个测点时，波形同时保存到 `baseline` 组和 `points/point_001` 组

**手动设置基准点**：
```python
def set_baseline_point(exp_id, point_id):
    """设置基准测点
    
    Args:
        exp_id: 实验ID
        point_id: 测点ID
    
    Returns:
        dict: {"success": bool, "message": str}
    """
    try:
        # 1. 检查测点是否已测量
        point_data = get_point_data(exp_id, point_id)
        if point_data['status'] != 'measured':
            return {
                'success': False, 
                'message': f'测点 #{point_id} 尚未测量，无法设为基准点'
            }
        
        # 2. 加载测点波形
        waveform = load_point_waveform(exp_id, point_id)
        
        # 3. 验证波形质量
        quality_check = validate_baseline_quality(waveform)
        if not quality_check['is_valid']:
            return {
                'success': False,
                'message': f'波形质量不合格：{quality_check["reason"]}'
            }
        
        # 4. 保存为基准波形
        save_baseline_waveform(exp_id, point_id, waveform)
        
        # 5. 更新数据库
        update_experiment_baseline(exp_id, point_id)
        
        # 6. 重新计算所有已测点的应力值
        recalculate_all_stress_values(exp_id)
        
        return {
            'success': True,
            'message': f'已将测点 #{point_id} 设为基准点，所有应力值已重新计算'
        }
        
    except Exception as e:
        return {'success': False, 'message': f'设置基准点失败：{str(e)}'}
```

#### 基准波形质量验证

```python
def validate_baseline_quality(waveform):
    """验证基准波形质量
    
    质量标准：
    1. 信噪比 (SNR) ≥ 20 dB
    2. 峰值幅度在合理范围内 (0.1V ~ 5V)
    3. 无明显失真或截断
    4. 波形长度符合预期
    
    Args:
        waveform: 波形数据 {"time": array, "voltage": array}
    
    Returns:
        dict: {
            "is_valid": bool,
            "snr": float,
            "peak_amplitude": float,
            "reason": str  # 不合格原因
        }
    """
    voltage = waveform['voltage']
    
    # 1. 计算信噪比
    snr = calculate_snr(voltage)
    
    # 2. 检查峰值幅度
    peak_amplitude = np.max(np.abs(voltage))
    
    # 3. 检查是否截断
    is_clipped = (np.max(voltage) >= 4.9) or (np.min(voltage) <= -4.9)
    
    # 4. 检查波形长度
    expected_length = 12000000  # 12M 采样点
    actual_length = len(voltage)
    length_ok = abs(actual_length - expected_length) < 1000
    
    # 综合判断
    checks = {
        'snr': snr >= 20.0,
        'amplitude': 0.1 <= peak_amplitude <= 5.0,
        'not_clipped': not is_clipped,
        'length': length_ok
    }
    
    is_valid = all(checks.values())
    
    # 生成不合格原因
    reasons = []
    if not checks['snr']:
        reasons.append(f'信噪比过低 ({snr:.1f} dB < 20 dB)')
    if not checks['amplitude']:
        reasons.append(f'峰值幅度异常 ({peak_amplitude:.2f} V)')
    if not checks['not_clipped']:
        reasons.append('波形截断')
    if not checks['length']:
        reasons.append(f'波形长度异常 ({actual_length} 点)')
    
    return {
        'is_valid': is_valid,
        'snr': snr,
        'peak_amplitude': peak_amplitude,
        'reason': '; '.join(reasons) if reasons else '合格'
    }
```

#### 基准波形加载

```python
def get_baseline_waveform(exp_id):
    """加载基准波形
    
    Args:
        exp_id: 实验ID
    
    Returns:
        dict: {
            "waveform": {"time": array, "voltage": array},
            "point_id": int,
            "captured_at": str,
            "quality": dict
        }
    """
    file_path = f'data/uniaxial_field/{exp_id}.h5'
    
    with h5py.File(file_path, 'r') as f:
        if 'baseline' not in f:
            raise ValueError(f'实验 {exp_id} 未设置基准波形')
        
        baseline_group = f['baseline']
        
        waveform = {
            'time': baseline_group['waveform']['time'][:],
            'voltage': baseline_group['waveform']['voltage'][:],
            'sample_rate': baseline_group['waveform'].attrs['sample_rate']
        }
        
        point_id = baseline_group.attrs['point_id']
        captured_at = baseline_group.attrs['captured_at']
        
        # 重新验证质量
        quality = validate_baseline_quality(waveform)
        
        return {
            'waveform': waveform,
            'point_id': point_id,
            'captured_at': captured_at,
            'quality': quality
        }
```

#### 更换基准波形

```python
def change_baseline_point(exp_id, new_point_id):
    """更换基准测点并重新计算所有应力值
    
    工作流程：
    1. 验证新基准点的波形质量
    2. 更新 HDF5 中的 baseline 组
    3. 更新数据库中的 baseline_point_id
    4. 重新计算所有已测点的时间差和应力值
    5. 更新云图
    
    Args:
        exp_id: 实验ID
        new_point_id: 新基准点ID
    
    Returns:
        dict: {
            "success": bool,
            "message": str,
            "recalculated_points": int
        }
    """
    try:
        # 1. 设置新基准点
        result = set_baseline_point(exp_id, new_point_id)
        if not result['success']:
            return result
        
        # 2. 重新计算所有已测点
        n_recalculated = recalculate_all_stress_values(exp_id)
        
        # 3. 更新云图
        update_contour_map(exp_id)
        
        return {
            'success': True,
            'message': f'基准点已更换为 #{new_point_id}，{n_recalculated} 个测点已重新计算',
            'recalculated_points': n_recalculated
        }
        
    except Exception as e:
        return {
            'success': False,
            'message': f'更换基准点失败：{str(e)}',
            'recalculated_points': 0
        }

def recalculate_all_stress_values(exp_id):
    """重新计算所有已测点的应力值
    
    Args:
        exp_id: 实验ID
    
    Returns:
        int: 重新计算的测点数量
    """
    # 1. 加载新基准波形
    baseline_data = get_baseline_waveform(exp_id)
    baseline_wf = baseline_data['waveform']
    
    # 2. 加载标定数据
    calibration = load_calibration_data(exp_id)
    
    # 3. 获取所有已测点
    measured_points = get_measured_points(exp_id)
    
    # 4. 逐个重新计算
    for point in measured_points:
        # 加载测点波形
        point_wf = load_point_waveform(exp_id, point['id'])
        
        # 重新计算时间差
        time_diff = calculate_cross_correlation(baseline_wf, point_wf)
        
        # 重新计算应力值
        stress = calculate_stress(time_diff, calibration)
        
        # 更新数据库
        update_point_analysis(exp_id, point['id'], time_diff, stress)
        
        # 更新 HDF5 中的 analysis 组
        update_hdf5_analysis(exp_id, point['id'], time_diff, stress)
    
    return len(measured_points)
```

#### 前端界面

**基准波形管理面板**（在左侧控制面板中）：

```html
<!-- 基准波形管理 -->
<div class="baseline-panel">
    <h3>基准波形</h3>
    
    <div class="baseline-info">
        <div class="info-row">
            <span class="label">当前基准点:</span>
            <span class="value" id="baseline-point-id">#1</span>
        </div>
        <div class="info-row">
            <span class="label">采集时间:</span>
            <span class="value" id="baseline-time">2025-12-19 14:30:15</span>
        </div>
        <div class="info-row">
            <span class="label">信噪比:</span>
            <span class="value quality-good" id="baseline-snr">28.5 dB</span>
        </div>
        <div class="info-row">
            <span class="label">质量评分:</span>
            <span class="value quality-good" id="baseline-quality">优秀 (0.98)</span>
        </div>
    </div>
    
    <div class="baseline-actions">
        <button id="view-baseline-btn" class="btn-secondary">
            查看波形
        </button>
        <button id="change-baseline-btn" class="btn-secondary">
            更换基准点
        </button>
    </div>
    
    <div class="baseline-warning" id="baseline-warning" style="display: none;">
        ⚠️ 基准波形质量较差，建议重新采集
    </div>
</div>
```

**更换基准点对话框**：

```javascript
async function showChangeBaselineDialog() {
    const measuredPoints = getMeasuredPoints();
    
    const html = `
        <div class="dialog-content">
            <h3>更换基准测点</h3>
            
            <p class="warning-text">
                ⚠️ 更换基准点后，所有已测点的应力值将重新计算
            </p>
            
            <div class="form-group">
                <label>选择新基准点:</label>
                <select id="new-baseline-select">
                    ${measuredPoints.map(p => `
                        <option value="${p.id}">
                            #${p.id} - (${p.x}, ${p.y}) - SNR: ${p.snr} dB
                        </option>
                    `).join('')}
                </select>
            </div>
            
            <div class="form-group">
                <label>
                    <input type="checkbox" id="confirm-recalc">
                    我理解所有应力值将重新计算
                </label>
            </div>
            
            <div class="dialog-actions">
                <button id="confirm-change-btn" class="btn-primary" disabled>
                    确认更换
                </button>
                <button id="cancel-change-btn" class="btn-secondary">
                    取消
                </button>
            </div>
        </div>
    `;
    
    showDialog(html);
    
    // 启用/禁用确认按钮
    document.getElementById('confirm-recalc').addEventListener('change', (e) => {
        document.getElementById('confirm-change-btn').disabled = !e.target.checked;
    });
    
    // 确认更换
    document.getElementById('confirm-change-btn').addEventListener('click', async () => {
        const newPointId = document.getElementById('new-baseline-select').value;
        
        showLoading('正在更换基准点并重新计算...');
        
        const result = await pywebview.api.change_baseline_point(
            currentExpId, 
            parseInt(newPointId)
        );
        
        hideLoading();
        
        if (result.success) {
            showSuccess(result.message);
            refreshExperimentData();
            closeDialog();
        } else {
            showError(result.message);
        }
    });
}
```

#### 基准波形质量问题处理

**场景1：基准波形质量差**
```
问题：信噪比 < 20 dB，或波形截断

处理流程：
1. 系统自动检测并显示警告
2. 建议用户：
   - 检查探头耦合
   - 调整示波器增益
   - 重新采集基准点
3. 用户可选择：
   - 重新采集当前基准点
   - 选择其他测点作为基准点
```

**场景2：基准点位置不合适**
```
问题：基准点在应力集中区域，不适合作为参考

处理流程：
1. 用户在采集过程中发现问题
2. 点击"更换基准点"
3. 选择应力较小的测点（如边缘点）
4. 系统自动重新计算所有应力值
```

**场景3：基准点未测量**
```
问题：用户跳过了第一个测点，导致无基准波形

处理流程：
1. 系统检测到无基准波形
2. 提示用户："请先采集至少一个测点作为基准"
3. 用户采集任意测点后，自动设为基准点
4. 继续采集其他测点
```

#### HDF5 存储结构

```python
# FIELD001.h5 中的 baseline 组
{
    'baseline': {
        'point_id': 1,  # 基准点ID（属性）
        'captured_at': '2025-12-19 14:30:15',  # 采集时间（属性）
        'waveform': {
            'time': [array],      # 时间轴
            'voltage': [array],   # 电压值
            'sample_rate': 1e9    # 采样率（属性）
        }
    }
}
```

#### 数据库字段

```sql
-- field_experiments 表中的基准波形相关字段
CREATE TABLE field_experiments (
    ...
    baseline_point_id INTEGER,        -- 基准测点ID
    baseline_stress REAL,             -- 基准应力值（可选，通常为0）
    baseline_captured_at TEXT,        -- 基准波形采集时间
    baseline_snr REAL,                -- 基准波形信噪比
    baseline_quality_score REAL,      -- 基准波形质量评分
    ...
);
```

### 6.5 异常处理

#### 异常类型
1. **示波器通信失败**：重试3次，失败后提示用户
2. **波形质量差**：信噪比过低，建议重测
3. **时间差异常**：超出合理范围，标记为可疑数据
4. **探头位置错误**：用户放错位置，允许重测
5. **基准波形缺失**：提示用户先设置基准点
6. **基准波形质量差**：建议重新采集或更换基准点

#### 处理策略
```python
def handle_measurement_error(point_id, error_type):
    """处理测量异常"""
    if error_type == 'communication_error':
        # 重试3次
        for i in range(3):
            if retry_capture(point_id):
                return 'success'
        return 'failed'
    
    elif error_type == 'low_snr':
        # 提示用户检查探头耦合
        show_warning("信号质量差，请检查探头耦合")
        return 'retry'
    
    elif error_type == 'abnormal_value':
        # 标记为可疑，允许继续
        mark_as_suspicious(point_id)
        return 'continue'
    
    elif error_type == 'baseline_missing':
        # 提示用户设置基准点
        show_error("未设置基准波形，请先采集至少一个测点")
        return 'abort'
    
    elif error_type == 'baseline_poor_quality':
        # 建议更换基准点
        show_warning("基准波形质量较差，建议更换基准点")
        return 'continue'
```

---

**📄 文档说明**：本文档为第2部分，包含第4-6章内容。  
**⬅️ 上一部分**：[单轴应力检测模块设计文档-第1.5-B部分.md](./单轴应力检测模块设计文档-第1.5-B部分.md)  
**➡️ 下一部分**：[单轴应力检测模块设计文档-第3部分.md](./单轴应力检测模块设计文档-第3部分.md)
